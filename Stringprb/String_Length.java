package Stringprb;

import javax.sql.rowset.serial.SerialStruct;
import java.util.Scanner;

public class String_Length {
    public void lenofstring(){
        Scanner sc = new Scanner(System.in);
        String word= sc.nextLine();
        int len=word.length();
        System.out.println(len);

    }
}
